---
- hosts: localhost
  vars_files:
    - host_vars/vars.yaml
  gather_facts: true

  roles:
    - role: common
    - role: images
    - role: volumes
    - role: security-group
    - role: create_instance

- hosts: CCC_Assignment_2
  vars_files:
    - host_vars/server_vars.yaml
  gather_facts: true
  roles:
    - role: server_environments